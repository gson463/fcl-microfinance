# Copy to .env and fill values from Supabase → Settings → API. Restart dev server after changes.

VITE_SUPABASE_URL=https://your-project-ref.supabase.co
VITE_SUPABASE_ANON_KEY=your-anon-public-key

# Server-side only (never expose in Vite client): for scripts/seed-attendance-demo.mjs
# SUPABASE_SERVICE_ROLE_KEY=your-service-role-key
# DEMO_OFFICER_EMAIL=officer@example.com
